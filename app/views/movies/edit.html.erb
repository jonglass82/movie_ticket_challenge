<div class="container">

<h1>Update your movie listing here</h1>

   <form action="/movie/update/<%= @movie.id %>" method="patch">

        <div class="form-group">
          <label for="title">Movie Title:</label>
          <input name="title" class="form-control" id="title" aria-describedby="title" value="<%= @movie.title %>">
        </div>

         <div class="form-group">
          <label for="show_time">Show Time:</label>
          <input name="show_time" class="form-control" id="show_time" aria-describedby="show_time" value="<%= @movie.show_time %>">
        </div>

        <div class="form-group">
          <label for="auditorium">Location:</label>
          <input name="auditorium" class="form-control" id="auditorium" aria-describedby="auditorium" value="<%= @movie.auditorium_id %>">
        </div>

        <div class="form-group">
          <label for="available_tickets">Available tickets:</label>
          <input name="available_tickets" class="form-control" id="available_tickets" aria-describedby="available_tickets" value="<%= @movie.available_tickets %>">
        </div>

        <h3>Remove this movie listing from your store? By selecting 'yes', you will discontinue the sales of this movie and it will not be visible in your showtimes. The sales to date will still be recorded in your dashboard.</h3>

        <div class="form-check form-check-inline">
          <input class="form-check-input" type="radio" name="active" id="remove" value="false">
          <label class="form-check-label" for="remove">Yes, please remove this listing from my store</label>
        </div>

        <br>
        <br>

        <button type="submit" class="btn btn-primary">Save</button>

    </form>

</div>